cargo-features = ["edition2024"]

[package]
name = "blueshift-vault"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
pinocchio = "0.10.1"
pinocchio-system = "0.5.0"
solana-address = { version = "2.0.0", features = ["curve25519"] }
